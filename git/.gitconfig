# This is Git's per-user configuration file.
[user]
  # Please adapt and uncomment the following lines:
	name = kortin.zhou
	email = hi@kortin.cn
	signingKey =

[core]
    ignorecase = false
    pager = delta
    editor = code --wait

[alias]
    st = status
    c = commit -m
    cm = commit -m
    cam = commit -am
    a = commit --amend
    an = commit --amend --no-edit
    cp = cherry-pick
    cpx = cherry-pick -x
    br = branch
    cbr = "! git branch --show-current | tee /dev/tty | pbcopy"
    co = checkout
    sw = switch
    swc = switch -c
    push-for = "! git push origin HEAD:refs/for/$(git branch --show-current)"
    sh = "! git add . && git stash"
    sp = stash pop
    mr = merge
    mb = merge-base
    rb = rebase -i
    undo = reset --soft HEAD^
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    ls = log --color --abbrev-commit --pretty=format:'[%C(red)%h%C(reset)] -%C(auto)%d%C(reset) %s %C(blue)(%cr) %C(bold magenta)<%an>%C(reset)'
    lt = ls-tree -r HEAD~ --name-only
    rss = reset --soft
    pro = pull -r origin
    ui = !lazygit
    last-commit = !echo "$(git log -1 --pretty=%B)"
    gh-clone = "! [ $# -ne 1 ] && echo 'Args: USERNAME/REPO_NAME' || git clone https://github.com/$1.git"
	ignore = "!gi() { curl -sL https://www.toptal.com/developers/gitignore/api/$@ ;}; gi"

[color]
	ui = auto

[init]
	defaultBranch = master

[commit]
	gpgSign = false

[commit]
	default = current

[tag]
	forceSignAnnotated = false

[stash]
	showStat = true

[merge]
	tool = Kaleidoscope
    conflictstyle = diff3

[mergetool]
	prompt = false

[mergetool "Kaleidoscope"]
	cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
	trustExitCode = true

[diff]
	tool = Kaleidoscope
    colorMoved = default

[difftool]
	prompt = false
	trustExitCode = true

[difftool "Kaleidoscope"]
	cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"

[interactive]
    diffFilter = delta --color-only --features=interactive

[delta]
    navigate = true    # use n and N to move between diff sections
    light = true
    side-by-side = true
    line-numbers = true
    hyperlinks = true
    hyperlinks-file-link-format = "vscode://file/{path}:{line}"

[gpg]
	format = openpgp
	program = gpg

[gpg "ssh"]
	program = ssh-keygen
	allowedSignersFile =

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[includeIf "gitdir:/Users/kortin/WorkSpace/baidu/"]
	path = /Users/kortin/WorkSpace/baidu/.gitconfig
[oh-my-zsh]
	git-commit-alias = 9e2c1548c3dfeefd055e1c6606f66657093ae928
